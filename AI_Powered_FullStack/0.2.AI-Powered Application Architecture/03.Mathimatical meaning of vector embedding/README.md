Read my existing README/file carefully before making any changes.

I already have the content organized into **8 main sections**, and each main section contains its own subtitles and detailed content.

### Your task

Do NOT invent new main sections.

Do NOT assume or manually define what the 8 sections are.

Instead:

1. Read the existing file.
2. Identify the **8 main sections/headings** from the structure that already exists.
3. Keep the existing content and subheadings under each section.
4. Create a clean **Table of Contents** near the top of the README.
5. Make every item in the Table of Contents clickable.
6. Each clickable item must link to the corresponding section **inside this same README** using Markdown heading anchors.
7. Do NOT create links to Google, websites, or any external page.

### Table of Contents appearance

I want the 8 main sections displayed using **numbers only**, with a relevant and interesting icon/emoji.

For example, the structure should look similar to:

* 🔢 **①**
* 🧭 **②**
* 🧩 **③**
* 📐 **④**
* 🧠 **⑤**
* 📊 **⑥**
* 🔗 **⑦**
* 🚀 **⑧**

However, choose the icons yourself based on the actual meaning of each section. The icons should be relevant to the content rather than random.

Do NOT write the full section titles in the Table of Contents. Keep the navigation visually simple and numbered.

### Important linking behavior

The links must point to the corresponding headings in the same README.

For example, if the fourth main section is detected from my existing file, clicking:

`📐 ④`

must take the reader directly to the **fourth section's detailed content**.

It must NOT:

* open Google
* open another website
* open a new page
* search for the title
* link to an external resource

It should use an internal Markdown anchor such as:

```markdown
[📐 ④](#the-correct-anchor-for-section-four)
```

Use the **actual heading from my file** to determine the correct GitHub anchor.

### Main section headings

For the actual detailed sections, keep the existing section content.

If the current headings contain long titles, preserve their meaning, but add the corresponding number and relevant icon to the main heading if this improves consistency.

For example:

```markdown
## 📐 ④ [Existing Main Section Title]
```

Then all of that section's existing subtitles and explanations should remain underneath it.

### Very important

The Table of Contents and the detailed sections must be connected correctly:

`📖 Table of Contents`
↓
`①` → detailed section ①
`②` → detailed section ②
`③` → detailed section ③
`④` → detailed section ④
`⑤` → detailed section ⑤
`⑥` → detailed section ⑥
`⑦` → detailed section ⑦
`⑧` → detailed section ⑧

When I click any number, it should scroll/jump directly to that section in the same README.

### Do not change my learning content

Do not rewrite, remove, or replace my explanations unless necessary to fix the Markdown structure.

Preserve:

* existing main sections
* existing subtitles
* formulas
* examples
* explanations
* code examples
* mathematical notation

Your main job is to improve the **README structure, navigation, readability, and internal linking**.

### Final check

Before finishing, verify every Table of Contents link.

Click/check conceptually that:

`① → section ①`

`② → section ②`

`③ → section ③`

`④ → section ④`

`⑤ → section ⑤`

`⑥ → section ⑥`

`⑦ → section ⑦`

`⑧ → section ⑧`

Make sure there are **no broken anchors and no external links** used for the main-section navigation.

Keep the README clean, professional, visually interesting, and beginner-friendly.
